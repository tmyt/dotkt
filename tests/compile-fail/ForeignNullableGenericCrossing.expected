# The refusal must name the MEMBER, the SLOT and the declaration no Kotlin expression inhabits — a message that
# only said "unsupported signature" would not tell the author which call to change or why. It must NOT offer
# constructing the .NET type by hand, which erases its own argument the same way and ends where it started.
the .NET member 'fgn.Api.CountPresent' declares
parameter 0
which NO Kotlin expression inhabits
the Kotlin physical image is 'System.Collections.Generic.List<object>'
unrelated CLR types
Change the .NET surface
