# The refusal names the declaration, the storage role, the offending type and the suspension it spans.
suspend-lowering: in `SuspendLocalByRefLikeLiveAcrossKt.cfLiveAcross`
the local variable
which is byref-like (a `ref struct`, via `System.Span`)
lives across the suspending call to `cfTick`
(mirrors C# CS4007)
