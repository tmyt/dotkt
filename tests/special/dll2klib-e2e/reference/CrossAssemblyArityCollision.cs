namespace Probe.Contracts;

// Together with Probe.Contracts.CrossAssemblyArity<T>, this makes the projected Kotlin classifier name depend on
// the complete batch universe. External delegate decoding must retain that same universe.
public sealed class CrossAssemblyArity
{
}
